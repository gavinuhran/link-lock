# link-lock
LinkLock is a strict single-link iOS viewer that opens a URL in an in-app WKWebView and blocks all navigation beyond the original resource. Users can scroll, play media, and interact with the page, but cannot move to other pages, domains, or apps—giving access to what’s needed without unlocking the entire internet.
